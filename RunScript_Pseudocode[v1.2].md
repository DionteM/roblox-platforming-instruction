### RunScript - Pesudocode 
Create a `local` variable named `UserInputService` and set it to the following. `game` is an global instance it has a function inside of it called `:GetService()` use this function on the instance with `"UserInputService"` as an input
